name: lx/lx-doc

autoload:
  psr-4:
    lx\doc\: ''

service:
  class: lx\doc\Service

  router:
    type: map
    routes:
      parser: {module: docParser}

  modules: module
  models: model
  modelCrudAdapter: \lx\DbCrudAdapter
